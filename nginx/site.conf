server {
    listen 80;
    listen 443 ssl;
    server_name 0xy.fr
                www.0xy.fr;
    
    ssl_certificate     /var/www/magictintin/certs/xyc;
    ssl_certificate_key /var/www/magictintin/certs/xyk;

    root /var/www/magictintin/0xy.fr;

	index index.php;# index.html;

	location / {
	    index index.php;
	    try_files $uri $uri/ @php_fallback;
	}

	location @php_fallback {
        rewrite ^(.+)$ $1.php last;
    }

    include /var/www/magictintin/php.conf;
}


server {
    listen 80;
    listen 443 ssl;
    server_name ms.0xy.fr
                www.ms.0xy.fr;
    
    ssl_certificate     /var/www/magictintin/certs/xyc;
    ssl_certificate_key /var/www/magictintin/certs/xyk;

    root /var/www/magictintin/micasend/webserver;

	index index.php;# index.html;

	location / {
	    index index.php;
	    try_files $uri $uri/ @php_fallback;
	}

	location @php_fallback {
        rewrite ^(.+)$ $1.php last;
    }

    include /var/www/magictintin/php.conf;
}
